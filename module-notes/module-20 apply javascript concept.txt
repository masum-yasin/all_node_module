* variable types....
	1. let = changeable - price , time etc.
	2. cons = unchangeable - name,number etc

* remainder....
	200 % 2 == 0 ;		=( 200 / 2 ) we vaagshesh/remainder 0 hole even/jor number nahole odd/bijor .


=================== math.xyz ======================

1. const myNumber = -5
   const output	= math.abs(myNumber);		= return the absolute/positive number.

2. const myNumber = 6.0398
   const output	= math.ceil(myNumber);		= return the greater integer number (7).

3. const myNumber = 6.0398
   const output	= math.floor(myNumber);		= return the smaller integer number (6).

4. const myNumber = 6.80
   const output	= math.round(myNumber);		= return the nearest integer number (7).

5. const myNumber = 21.80
   const output	= math.random();		= 0 - 1 porjonto random number dibe.
   const output	= math.random() * 10;		= 0 - 10. 


6. For of loop

    for (const element of names) {
     console.log(element); 		= aikhane array er elemnet gula akta akta kore niye ashbe .(like for loop)
}

7. (string) ke (array) er moto index , length dekha jay. Change kora jay na.